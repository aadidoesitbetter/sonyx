# Sonyx Music Bot — requirements
# Core
discord.py[voice]>=2.3.0
yt-dlp>=2025.1.15
python-dotenv>=1.0.0
mutagen>=1.47.0
PyNaCl>=1.5.0
static-ffmpeg>=2.5

# Lyrics (no API key required)
syncedlyrics>=0.4.0

# Optional: Spotify support (set SPOTIFY_CLIENT_ID + SPOTIFY_CLIENT_SECRET in env)
spotipy>=2.23.0
